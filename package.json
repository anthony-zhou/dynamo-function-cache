{
  "name": "dynamo-function-cache",
  "version": "1.0.0",
  "description": "1. Create a Serverless account and make sure AWS is configured as a provider.  2. Install Serverless CLI globally",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "aws-sdk": "^2.871.0",
    "dotenv": "^8.2.0"
  }
}
